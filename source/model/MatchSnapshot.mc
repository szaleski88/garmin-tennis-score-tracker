class MatchSnapshot {
    var state;
    var settings;
    var history;

    function initialize(matchState, matchSettings, matchHistory) {
        state = matchState;
        settings = matchSettings;
        history = matchHistory;
    }
}
